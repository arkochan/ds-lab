#include "bits/stdc++.h"

using namespace std;

int arr[600] = {-1};
int main()
{
    long long i, j, n;
    long long x = 0, t, inp, sum = 0;
    for (auto ss : arr)

        ss = -1;

    for (auto ss : arr)

        cout << INT_MIN << endl;
}
